displayName: Request ID
type: middleware
import: github.com/orykami/traefik-request-id
summary: Generates a unique X-Request-ID header (UUID v4) for each incoming request if not already present.
testData:
  headerName: X-Request-ID
